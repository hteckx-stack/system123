rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all authenticated users to read/write to all core collections.
    // This is appropriate for an internal admin-only portal where all users are staff.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
